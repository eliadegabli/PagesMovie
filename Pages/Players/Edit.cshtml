@page
@model PagesMovie.Pages.Players.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Player</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Player.ID" />
            <div class="form-group">
                <label asp-for="Player.playerName" class="control-label"></label>
                <input asp-for="Player.playerName" class="form-control" />
                <span asp-validation-for="Player.playerName" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Player.inGame" /> @Html.DisplayNameFor(model => model.Player.inGame)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="Player.Rating" class="control-label"></label>
                <input asp-for="Player.Rating" class="form-control" />
                <span asp-validation-for="Player.Rating" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
